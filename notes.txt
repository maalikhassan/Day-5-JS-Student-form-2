1)document.querySelector("#submitbtn").addEventListener("click", function() {
    alert("Button clicked!");
}); //querySelector("#submitbtn") is an alternative to getElementById("submitbtn") that can capture both ids and classes

2)` Name:${txtName} ` //this is better wy to use string concatenation, called String interpolation, better to handle complex strings
` Age:${document.getElementById("age").value

use backtick

3)//install agent cli 
C:\Users\Dell>npm install -g @google/gemini-cli

gemini

4)crwating object -

const customer = {
    name: txtName,
    email: txtEmail,
    phone: txtPhone,

};

 let customerlist=[]

    customerlist.push(customer)

    console.log(customer)


5)Sweetalert code -

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
Swal.fire({
    title: 'Success!',
    text: 'Your form has been submitted.',
    icon: 'success',
    confirmButtonText: 'OK'
});

6) if string block have to use backtick `` , can t use ""

7)for arays referce we use const instead of 'let'

8) Browser also has storage , (cookies session, local storage etc..)
localstorage.setitem(key,value)

9)cant pass object in this manner, so we use JSON.Stringify (object)